<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Selected state -->
    <item android:state_checked="true"
        android:drawable="@drawable/home_selscted" />
    <!-- Default (unselected) state -->
    <item android:drawable="@drawable/home_icon" />
</selector>
